# Assets / Inventory / Project Accounting (ZS-SVC-G-001) — Build Findings

This document tracks the from-scratch build of the domain defined in `docs/original_doc/ZoikoSuite_Assets_Inventory_Project_Accounting_Detailed_Service_Specifications_v1_0.docx` — 12 capabilities across three sub-domains (Assets AST-01..03, Inventory INV-01..05, Project Accounting PRJ-01..04). A fresh audit on 2026-09-08 confirmed zero prior implementation anywhere in this repository.

Per the doc's own §7: *"Physical deployment may be consolidated, but each logical service retains separate authoritative facts, commands, states, evidence and certification requirements."* This build uses 3 new services, one per sub-domain: `asset-management-svc` (AST-01/02/03), `inventory-management-svc` (INV-01..05), `project-accounting-svc` (PRJ-01..04) — the same doctrine already applied to the Accounting domain (18 capabilities → 4 services), documented in `master-register-findings-2026-08-27.md`.

Each section below follows the same format established in that document: verbatim spec quotes, real negative-path enforcement, test counts, real-Postgres verification, negative-control description.

---

### §1 AST-01 Fixed Asset Register — built from scratch (2026-09-08)

New service `asset-management-svc` (port 8167, DB `asset_management`), scaffolded from `consolidation-svc`'s own layout (the smallest, most recent reference service in this repo).

**Authority matrix row (verbatim):** "AST-01 | FixedAsset | Posted GL balances, depreciation calculations or free-form carrying-value edits." Fuller ownership: "FixedAsset; AssetComponent; asset category/class; tag/serial; ownership entity; custodian/location references; acquisition/source references; in-service metadata; AssetBookProfile linkage; lifecycle projection. It does not own posted GL balances."

**State model (verbatim):** "Candidate→Reviewed→Registered→Capitalized/Active; Suspended/Disposed/HeldForSale are lifecycle dimensions driven by accepted events; book states tracked separately." No command in this doc's own command list reaches "Reviewed" independently of "Registered" — only `ApproveAssetRegistration` bridges Candidate to Registered — so this build collapses Reviewed into that one transition, documented in migration `000001`'s own comment, the same "no separate command exists for a named intermediate state" pattern already used in the Accounting domain (e.g. ACC-17's Planned/Loaded collapse). Disposed and HeldForSale are explicitly "driven by accepted events" — AST-03's own future authority — and are deliberately not reachable through any AST-01 command; migration `000001`'s own `CHECK` constraint on `status` excludes them for now.

**A structurally enforced negative path, not a runtime check:** "User edits carrying value directly" is satisfied by `fixed_assets` never having a carrying/net-book-value column at all — there is nothing to edit. Every other capability's own negative-path scenarios are real, tested guards:
- *"Asset capitalized without source evidence"* — `RequestCapitalization` refuses (422) unless `acquisition_source_ref` is recorded on the asset.
- *"Physical asset merged across legal entities"* — `MergeAssetControlled` checks both the source and target's real `legal_entity_id` against the actual rows (not just the caller's claim) before allowing the merge; refused otherwise.
- *"Retroactive book profile change rewrites prior depreciation"* — `AssignAssetBookProfile` never updates an existing (asset, book) assignment, only inserts a new one; a second assignment for the same book is refused by the real `UNIQUE(tenant_id, asset_id, book_id)` constraint, surfaced as a 422.

**Segregation of duties:** `ApproveAssetRegistration` refuses when the approver is the same principal who created the candidate — the spec's own words, "Creator cannot approve capitalization where maker-checker is required," the same posture as ACC-03's own submit/approve split. `MergeAssetControlled`/`SplitAssetControlled` share a single, deliberately distinct `ASSET_MERGE_SPLIT` authorization action, separate from every other asset action — the spec's own "merge/split requires independent approval and lineage preservation."

**Lineage preservation, not deletion:** `MergeAssetControlled` marks the source asset `MERGED` with `merged_into_asset_id` set, never deletes it. `SplitAssetControlled` creates a new `CANDIDATE` asset recording `split_from_asset_id`, and marks the named components `moved_to_asset_id` rather than deleting or copying them — both directions of `GetAssetSourceLineage` (not yet built as a separate query; served today by reading these same columns off `GetAsset`) are answerable from permanent, real rows.

22 new tests: 15 handler tests (`handler_test.go` — missing fields, self-approval refusal, capitalization-without-evidence refusal, the retroactive book-profile refusal, suspend-requires-reason and suspend-from-CANDIDATE refusal, both merge negative/positive paths, both split negative paths, the metadata-amend-never-changes-status SoD proof, and an authorization-denied case) plus 7 real-Postgres tests (`pg_store_test.go` — create/get round-trip, a wrong-status transition refusal, the full Candidate→Active→Suspended→Active lifecycle, the real `UNIQUE` constraint behind the retroactive-book-profile refusal, the real cross-legal-entity merge refusal against actual rows, the component-move transaction behind split, and RLS tenant isolation). Verified against a real throwaway Postgres container: migration `000001` applies cleanly, all three tables and their constraints confirmed live, `go build`/`go vet` clean on first attempt. Negative-controlled twice: temporarily removed the `UNIQUE(tenant_id, asset_id, book_id)` constraint (confirmed the retroactive-book-profile test failed, differently than expected but load-bearing all the same — restored), and temporarily disabled the cross-legal-entity check in `MergeAssets` (confirmed the merge-refusal test failed exactly as expected, then restored).

**Deliberately out of scope, stated honestly:** `GetAssetAsOf` (point-in-time query) and `GetAssetSourceLineage` as a dedicated endpoint are not yet built — the underlying data (`split_from_asset_id`, `merged_into_asset_id`, `acquisition_source_ref`) is all real and queryable via `GetAsset`, but no separate as-of or lineage-trace endpoint exists yet. `ListAssets`'s own pagination is unbounded — this v1 has no equivalent of `general-ledger-svc`'s `DefaultListLimit`/`MaxListLimit` yet. Both are real, named gaps to close in a follow-up pass, not silent omissions. AST-02 (Depreciation) and AST-03 (Asset Event) — the two capabilities this register's own Dependencies field for AST-01 names as depending on it — are the next capabilities in this same service.

---

### §2 AST-02 Depreciation — built from scratch (2026-09-08)

Same service, `asset-management-svc` — AST-02 is the second of the three capabilities co-located here per the doc's own §7 consolidation doctrine.

**Authority matrix row (verbatim):** "AST-02 | DepreciationSchedule | Asset physical identity, impairment/disposal decisions or direct GL posting." Fuller ownership: "DepreciationSchedule; DepreciationRun; DepreciationLine; accumulated-depreciation subledger facts by asset/component/book; run eligibility/exceptions; schedule version."

**Two independently-lifecycled state machines (verbatim):** "Schedule Draft→Validated→Active→Superseded; Run Draft→PopulationFrozen→Calculated→Validated→Approved→AccountingEventEmitted→Reconciled/Certified." The named commands don't cover every state 1:1 — documented in full in migration `000002`'s own comment: `BuildDepreciationSchedule` lands a schedule directly in `ACTIVE` (no command reaches Draft/Validated independently, the same collapse pattern as AST-01's own Candidate/Reviewed); `ValidateDepreciationRun` performs the actual calculation AND validates in one step (no command reaches Run's own `Calculated` independently, the same collapse ACC-10's Planned/Calculated already used); `Reconciled`/`Certified` have no command at all and are left unbuilt, stated honestly; `SupersedeDepreciationRun` is real despite the Run state model never naming "Superseded" — inferred from the spec's own negative path ("Rerun emits duplicate accounting event") to mean reversing an already-emitted run's journal, mirroring ACC-10 FX Revaluation's own "reversed via new run" pattern.

**A real "ACC-04" dependency, not a simulated one:** `EmitDepreciationAccountingEvent` posts through general-ledger-svc's own system-originated posting path (`POST /v1/postings/events`), keyed by the run's own ID as `source_event_id` — the spec's own negative path, "Rerun emits duplicate accounting event," is enforced by GL's real `UNIQUE(tenant_id, source_event_id)`, not reimplemented here. `SupersedeDepreciationRun` calls GL's own `CreateReversalPosting` (`/v1/postings/reversals`) — the same reversal primitive every other posted journal on this platform uses. A new `internal/clients` package (`ledger.go`) carries both calls; the handler refuses (503, not a nil-pointer crash) if no ledger client was configured.

**Every negative path enforced at its root, mostly via real database constraints, not application checks alone:**
- *"Same asset depreciated twice in period"* — enforced TWICE, independently: `idx_depreciation_schedules_current_asset_book`, a real `UNIQUE(tenant_id, asset_id, book_id) WHERE effective_to IS NULL`, means at most one current schedule can exist per asset/book at all; `idx_depreciation_runs_live_period`, a real `UNIQUE(tenant_id, legal_entity_id, fiscal_period) WHERE status != 'SUPERSEDED'`, means at most one live run can exist per entity/period. Composing the two makes a duplicate depreciation structurally unreachable, not merely checked for.
- *"Disposed component remains in eligible population"* — `FreezeDepreciationPopulation`'s own population query filters to `fixed_assets.status = 'ACTIVE'`. AST-03 (not yet built) will own the real `DISPOSED` status; because this filter already excludes every non-ACTIVE status today, once AST-03 introduces `DISPOSED` as a status an asset transitions OUT of `ACTIVE` into, this negative path continues to be enforced automatically — no AST-02 change will be needed when AST-03 ships. Tested today with `SUSPENDED` as the real, already-available stand-in exclusion case.
- *"Useful life changed after approval without invalidation"* — `RecalculateSchedule` has no in-place edit path to a schedule's own parameters at all: the current version is end-dated (`effective_to` set, status `SUPERSEDED`) and a new version created, the identical pattern this session already built for ACC-09's `SupersedeAllocationRule`.
- *"Rerun emits duplicate accounting event"* — see the real ACC-04 dependency above.

**Segregation of duties:** `ApproveDepreciationRun` refuses when the approver is the run's own creator — the spec's own words, "Run preparer cannot self-approve material exceptions." No severity/materiality concept exists in this platform, so — the same bootstrap-gap direction ACC-03 took for journal maker/checker — self-approval is refused universally, not selectively.

**Straight-line depreciation only, stated honestly:** the spec's own required input "method" is accepted but constrained to `STRAIGHT_LINE` by a real `CHECK` constraint — no other method is implemented in this v1. The last-period-absorbs-remainder rounding pattern this platform already uses (ACC-07/08/09's own recognition/allocation amount helpers) is intentionally NOT applied here: a schedule's own monthly amount is fixed and capped at the remaining `(cost − residual − accumulated)` balance instead, so a schedule that runs past its useful life in wall-clock terms (e.g. a late final run) never depreciates below its residual value.

31 new tests: 9 handler tests (`depreciation_test.go` — the asset-not-eligible refusal, both duplicate-schedule and duplicate-run negative paths, the recalculate-supersedes-old-version proof, the non-ACTIVE-asset population exclusion, a full build→run→freeze→validate→approve→emit lifecycle asserting the real journal ID lands on the run and GL is called exactly once, the self-approval refusal, and the supersede→journal-reversal→period-released round trip) plus 6 real-Postgres tests (`depreciation_store_test.go` — the two duplicate-negative-paths against actual constraints, the schedule supersession, the full lifecycle including the exact straight-line calculation and journal linkage, and the append-only trigger). Verified against a real throwaway Postgres container: migration `000002` applies cleanly on top of `000001`, and the full pre-existing AST-01 suite (15 handler + 7 store tests) still passes unmodified. One real bug caught during development, not by inspection: `depreciation_runs.status` was declared `VARCHAR(20)`, one character too narrow for `ACCOUNTING_EVENT_EMITTED` (24 characters) — the full lifecycle test failed with a real Postgres length-violation error the first time it reached that transition; widened to `VARCHAR(30)`. Negative-controlled twice: temporarily removed the schedule's own partial `UNIQUE` index (confirmed the duplicate-schedule test failed exactly as expected, restored), and temporarily removed the `depreciation_lines` append-only `UPDATE` trigger (confirmed the corresponding test failed exactly as expected, restored).

**Deliberately out of scope, stated honestly:** `Reconciled`/`Certified` (the Run state model's own terminal states) have no command anywhere in the spec and are not built. `ListRunExceptions` and `GetDepreciationAsOf` (two of the five named queries) are not yet built. AST-02's own five named events (`DepreciationScheduleBuilt`, `DepreciationRunCalculated`, `DepreciationRunApproved`, `DepreciationAccountingEventEmitted`, `DepreciationRunSuperseded`) are not yet wired into the `Publisher` interface AST-01 already established — a real, stated gap, not a silent one; nothing in AST-02's own negative-path table depends on them, so this doesn't block correctness, but it means downstream consumers (e.g. a future ACC-06 subledger reconciliation against depreciation) have nothing to subscribe to yet. Depreciation convention/calendar (mid-month vs. full-month, fiscal-vs-calendar alignment) is not modeled — every period is treated as one full month's worth of straight-line depreciation regardless of in-service date within the period, a simplification the spec's own "depreciation convention" server-resolved field anticipates but no REF service exists to supply yet.

---

### §3 AST-03 Asset Event — built from scratch (2026-09-08)

Same service, `asset-management-svc` — AST-03 is the third and last of the three Asset capabilities co-located here.

**Authority matrix row (verbatim):** "AST-03 | AssetEvent | General Ledger or unsupported valuation/legal conclusions." Purpose (verbatim): "Record governed economic lifecycle events that change asset book state, including capitalization, additions, component replacement, transfer, impairment/revaluation and disposal, while preserving immutable history." Fuller ownership: "AssetEvent; event type; affected asset/component/book; source basis; value/effective date; approval fingerprint; resulting book-state delta; correction/supersession links."

**State model (verbatim):** "Draft→Validated→Approved→Applied→AccountingEventEmitted→Reconciled; Rejected/Cancelled before apply; applied events corrected only by reverse/supersede." The same doc-vs-command mismatch pattern recurring throughout this build hits AST-03 twice: no command named `RejectAssetEvent`/`CancelAssetEvent` exists despite "Rejected/Cancelled" being named states — left unreachable in this v1, the same posture as AST-02's own `Reconciled`/`Certified` gap; and no command named `EmitAssetEventAccountingEvent` exists either, despite `AccountingEventEmitted` being its own named state — `ApplyAssetEvent` performs the book-state delta AND (where the event carries a $ amount) the ledger posting in one step, the same collapse `ValidateDepreciationRun` already applies to Run's own `Calculated` state (migration `000002`). `RecordDisposal`/`RecordImpairment`/`RecordRevaluation`/`RecordComponentReplacement` — four of the spec's own ten explicit commands — are built as thin, type-specific entry points into the one real create path (`CreateAssetEvent`), not four separate implementations.

**Scope narrowing, stated honestly up front:** of the seven event types the purpose statement names, only `DISPOSAL` drives a real, unambiguous book-state delta in this v1 — `ApplyAssetEvent` transitions `fixed_assets.status` `ACTIVE`/`SUSPENDED` → `DISPOSED`, the one status AST-01's own migration `000001` explicitly reserved for AST-03 ("Disposed ... driven by accepted events"). The other six event types (`CAPITALIZATION`, `ADDITION`, `COMPONENT_REPLACEMENT`, `TRANSFER`, `IMPAIRMENT`, `REVALUATION`) are recorded as permanent, evidenced, immutable history — this table's whole reason to exist — but do not themselves rewrite AST-01/AST-02 state yet: an applied `IMPAIRMENT` event does not reduce AST-02's own schedule `cost_basis`, and a `CAPITALIZATION` event does not itself drive AST-01's `RequestCapitalization`. Wiring each of those six into their own downstream mutation is real, non-trivial follow-on work per event type, deliberately deferred.

**All four of the spec's own Minimum Negative-Path Acceptance scenarios, enforced as real, tested guards — three of the four at the database layer, not merely in application code:**
- *"Impairment amount entered without evidence/approval"* — `ValidateAssetEvent` refuses (422) to move an `IMPAIRMENT` or `REVALUATION` event out of `DRAFT` without a recorded `valuation_evidence_ref`.
- *"Disposed asset event edited in place"* — enforced structurally by `trg_reject_asset_event_economic_mutation`: once an event's status is `APPLIED` or later, its own economic facts (`event_type`, `asset_id`, `amount`, `proceeds_amount`, `valuation_evidence_ref`, `source_document_ref`, `effective_date`, `currency`) can never be `UPDATE`d again, regardless of application-code bugs — a real Postgres trigger, not a documented convention. The only correction path for an applied event is `ReverseAssetEvent`/`SupersedeAssetEvent`, which touch status/reversal metadata only.
- *"Cross-entity asset transfer bypasses intercompany accounting"* — `chk_asset_event_no_cross_entity_transfer`, a real `CHECK` constraint, refuses at the database layer any `TRANSFER` event whose `destination_legal_entity_id` differs from the event's own `legal_entity_id`; the handler also refuses it before the store is even asked (`ErrCrossEntityTransferBlocked`), matching the spec's own failure semantics — "Direct cross-legal-entity transfer is blocked: ownership change requires intercompany/disposal-acquisition treatment" — this service does not attempt intercompany treatment itself, it simply blocks the unsafe path outright.
- *"Hard-closed-period event silently backdated"* — `ApplyAssetEvent` calls a new, real `financial-close-svc` dependency (`internal/clients.Clients.CheckPeriodOpen`, `GET /v1/close/periods/status`) before applying anything; a `LOCKED`/`CLOSED` period refuses (422, `ErrPeriodLocked`), and an unreachable `financial-close-svc` fails CLOSED (503, `ErrPeriodCheckUnavailable`) — mirrors `general-ledger-svc`'s own `internal/close.Client.CheckPeriodOpen` exactly, including the "a period this service has never heard of defaults to open" semantics.

**A real "ACC-04" dependency, shared with AST-02:** `ApplyAssetEvent` posts through general-ledger-svc's own `POST /v1/postings/events` (via the same `internal/clients.Clients`, extended rather than duplicated) whenever the event carries an `amount` and both a debit/credit account code, keyed by the event's own ID as `source_event_id` for idempotency. `ReverseAssetEvent`/`SupersedeAssetEvent` call the same `CreateReversalPosting` primitive AST-02's own `SupersedeDepreciationRun` already uses.

**Segregation of duties:** the spec's own words, "Event initiator cannot approve material impairment/revaluation/disposal where maker-checker applies" — `ApproveAssetEvent` refuses self-approval only for the three event types the spec itself names as material (`IMPAIRMENT`, `REVALUATION`, `DISPOSAL`); the other four carry no approval-time $ consequence in this v1, so same-principal approval is permitted for them — a narrower, spec-literal application of the maker/checker posture than AST-02's own universal refusal (no finer materiality concept exists elsewhere in this platform, but here the spec itself supplies one).

**Reverse and Supersede both revert the disposal delta:** if the original event was a `DISPOSAL`, both `ReverseAssetEvent` and `SupersedeAssetEvent` revert the asset's own `DISPOSED` status back to `ACTIVE` in the same transaction as the event's own status transition — an applied disposal's book-state consequence cannot outlive the event that caused it once that event is corrected.

22 new tests: 17 handler tests (`asset_event_test.go` — missing event_type, component-replacement-without-component, both cross-entity-transfer paths, both impairment-evidence paths, both material/non-material self-approval paths, the disposal→DISPOSED transition, disposing an already-disposed asset refused, both period-lock paths, the $-amount emit-with-journal path, the $0 apply-never-emits path, the disposal-reverse-reverts-to-ACTIVE round trip including the journal reversal call count, missing-reason refusal, and re-validating an already-validated event refused) plus 5 real-Postgres tests (`asset_event_store_test.go` — create/get round-trip, the real `chk_asset_event_no_cross_entity_transfer` constraint rejecting a raw insert, the full disposal lifecycle including the double-disposal and re-apply refusals, Reverse requiring `ACCOUNTING_EVENT_EMITTED` and reverting the asset status, and the reject-mutation trigger rejecting an economic-field `UPDATE` while still allowing a status-only one). Verified against a real throwaway Postgres container: migration `000003` applies cleanly on top of `000001`/`000002`, `fixed_assets`' extended `CHECK` constraint (adding `DISPOSED`) verified live, and the full pre-existing AST-01 (15+7) and AST-02 (9+6) suites still pass unmodified — 17/17 store tests green. Negative-controlled: temporarily neutered `trg_reject_asset_event_economic_mutation`'s own condition (`IF FALSE AND ...`), confirmed `TestPgStore_AssetEvent_RejectsEconomicMutationOnceApplied` failed exactly as expected, restored, confirmed green again (17/17).

**Deliberately out of scope, stated honestly:** six of the seven event types do not yet drive their own downstream AST-01/AST-02 state mutation (see scope narrowing above). `Reconciled` (the state model's own terminal state) and `RejectAssetEvent`/`CancelAssetEvent` have no command anywhere in the spec and are not built. `GetAssetBookStateAsOf` and `ExplainAssetState` (two of the five named queries) are not yet built as dedicated endpoints — `GetAssetEventChain`'s own data is served today by `ListAssetEvents`, real and queryable, just not yet framed as an "explain" narrative. AST-03's own seven named events (`AssetEventCreated`, `AssetEventApproved`, `AssetEventApplied`, `AssetImpaired`, `AssetRevalued`, `AssetDisposed`, `AssetEventReversed`) are not yet wired into the `Publisher` interface — the same stated gap AST-02 left open, not a silent one. TAX treatment, GOV linkage, and live BNK/AR integration for disposal proceeds (the spec's own dependency, "BNK/AR for disposal proceeds") are bootstrap gaps: `proceeds_amount` is captured as caller-declared evidence, with no live accounts-receivable-svc call — the same "deliberate bootstrap gap, safety-favoring direction" posture this session has applied consistently where a named dependency doesn't yet exist as a callable service.

This closes all three Asset capabilities (AST-01/02/03). Next: INV-01 (Item/Product Master), the first of five Inventory capabilities, in a new `inventory-management-svc`.

---

### §4 INV-01 Item / Product Master — built from scratch (2026-09-09)

New service, `inventory-management-svc` (port 8168, DB `inventory_management`) — the first of five Inventory capabilities, scaffolded from `consolidation-svc`'s own layout the same way `asset-management-svc` was.

**Authority matrix row (verbatim):** "INV-01 | InventoryItem | Commercial offer/pricing authority or historic movement reinterpretation." Purpose (verbatim): "Maintain canonical stockkeeping and inventory-accounting attributes for physical/stock items while remaining distinct from the commercial Product & Service Catalog." Fuller ownership: "InventoryItem; item/SKU identity; stocking/base UOM; item type; lot/serial/expiry policy; inventory classification; entity inventory profile; valuation-policy reference; lifecycle."

**State model (verbatim):** "Draft→Active→Suspended→Retired; valuation/tracking policy versions are effective-dated and do not rewrite historic movements." The same doc-vs-command mismatch pattern recurring throughout this build hits INV-01 too: no command named `SuspendInventoryItem`/`ReactivateInventoryItem` exists despite "Suspended" being a named state — left unreachable in this v1, the same posture as AST-02's own `Reconciled`/`Certified` gap. This v1's only real transitions are `DRAFT` → `ACTIVE` (`ActivateInventoryItem`) and `ACTIVE` → `RETIRED` (`RetireInventoryItem`).

**Versioned tracking/valuation policies:** `inventory_tracking_policies` and `inventory_valuation_policies` are versioned exactly like AST-02's own `DepreciationSchedule` — a stable logical `policy_id` carrying effective-dated versions, never mutated in place. `SetValuationPolicyFutureEffective`'s own name is the spec's own structural answer to its SoD requirement ("Retroactive valuation-policy change requires controlled migration/revaluation approval"): the handler refuses any `effective_from` that isn't strictly later than the moment the command runs, so there is no path to a retroactive change at all, not merely an unauthorized one.

**Minimum negative-path acceptance (verbatim, all four):**
- *"Commercial catalog changes inventory valuation method"* — `LinkCommercialCatalogItem`'s own request carries only a `catalog_item_id`; there is no code path from it to `inventory_valuation_policies` at all. It is also a distinct authz action (`INVENTORY_ITEM_MANAGE`) from `SetValuationPolicyFutureEffective` (`INVENTORY_POLICY_ASSIGN`) — the spec's own SoD, "commercial pricing/catalog editors cannot alter inventory accounting policy."
- *"Historic movement reinterpreted using new UOM mapping"* — `base_uom` is set once at `CreateInventoryItem` and is not a field of `AmendInventoryProfileRequest` at all (compile-time, not just a runtime check) — no command on this platform can change an item's base UOM after creation.
- *"Lot-tracked item moved without lot identity"* — INV-01 does not itself move stock; it records a real, queryable `RequiresLotTracking`/`RequiresSerialTracking`/`RequiresExpiryTracking` policy for INV-03 (not yet built) to check before accepting a movement. Stated honestly: the actual movement-time block is deferred to INV-03.
- *"Retired item accepts new receipt without override"* — `RETIRED` is a real terminal status (`ValidItemTransitions[RETIRED] = {}`, and the guarded `transitionItem` UPDATE only ever moves `ACTIVE` → `RETIRED`) — INV-03, once built, can refuse a receipt against a non-ACTIVE item.

**Failure/degradation semantics (verbatim):** "Missing base UOM, valuation policy or entity profile blocks stock movement/valuation." `base_uom` is `NOT NULL` at creation; `ActivateInventoryItem` additionally refuses (422, `ErrValuationPolicyRequiredForActivation`) an item with no current valuation policy — since only `ACTIVE` items are eligible for movement in this model, this is the one gate INV-01 itself owns for that failure mode.

**Dependencies (verbatim):** "BIZ-07; REF UOM; ORG; TAX; ACC mappings; GOV; INV-03/04." None of BIZ-07 (commercial catalog), REF UOM, or TAX exist as callable services on this platform yet — `catalog_item_id` and `base_uom` are caller-declared references, the same "deliberate bootstrap gap, safety-favoring direction" posture used throughout this session (e.g. AST-02's own account-code sourcing). INV-03/04 are this same service's own next capabilities, to be called in-process once built.

19 new tests: 14 handler tests (`handler_test.go` — missing-fields, duplicate-SKU, both activation-without/with-valuation-policy paths, both past/future valuation-policy-date paths, invalid valuation method, missing-reason retire, retire-from-DRAFT refused, retire-from-ACTIVE succeeds, catalog-link never changes valuation method, amend never changes base UOM, and an authorization-denied path) plus 5 real-Postgres tests (`pg_store_test.go` — create/get round-trip, the real `UNIQUE(tenant_id, legal_entity_id, sku)` constraint rejecting a duplicate, the guarded DRAFT→ACTIVE→re-activate-refused transition, tracking-policy versioning proving the old version is end-dated not mutated, and `GetProfileAsOf` proving a query for an instant between two policy versions returns the historic one — the concrete proof behind negative path #2). Verified against a real throwaway Postgres container (`postgres:16-alpine`, torn down after use): all 5 store tests pass. Negative-controlled: temporarily removed the `UNIQUE(tenant_id, legal_entity_id, sku)` constraint, confirmed `TestPgStore_CreateItem_DuplicateSKU_Refused` failed exactly as expected, restored, confirmed green again (5/5).

One real bug caught by the real-Postgres pass, not by inspection: `SetTrackingPolicy`/`SetValuationPolicy` never generated a `policy_id` for a brand-new (first-version) policy — the in-memory stub happily accepted an empty string, but real Postgres rejected it with `invalid input syntax for type uuid: ""` (SQLSTATE 22P02) against the `NOT NULL UUID` column. Fixed by generating a fresh UUID for `policy_id` whenever no prior version exists.

**Deliberately out of scope, stated honestly:** `Suspended` (the state model's own named state) has no command anywhere in the spec and is not built. `GetInventoryProfileAsOf` is built as a query parameter (`?at=`) on `GET /v1/items/{id}/profile-as-of` rather than a dedicated named endpoint per the spec's query list, but is otherwise fully real. INV-01's own four named events (`InventoryItemCreated`, `InventoryItemActivated`, `InventoryPolicyChanged`, `InventoryItemRetired`) ARE wired into the `Publisher` interface and fire on every real transition — unlike AST-02/AST-03, this gap does not apply here. `ItemType`/"stock/non-stock classification" is captured as a free-form string, not a closed enum — no REF service defines the canonical set yet.

Next: INV-02 (Inventory Location), the second of five Inventory capabilities, in the same service.

---

### §5 INV-02 Inventory Location — built from scratch (2026-09-09)

Same service, `inventory-management-svc` — INV-02 is the second of five Inventory capabilities co-located here.

**Authority matrix row (verbatim):** "INV-02 | InventoryLocation | Legal ownership changes or inventory quantity/value." Purpose (verbatim): "Maintain authoritative physical/custody inventory-location hierarchy and operational eligibility without inferring legal ownership from physical location." Fuller ownership: "InventoryLocation; warehouse/site/bin/quarantine/transit location; parent hierarchy; custody entity; operational state; location type; counting/negative-stock attributes."

**State model (verbatim):** "Draft→Active→Suspended/Quarantine→Retired; hierarchy changes versioned; physical state separate from item quantity." The same doc-vs-command mismatch pattern recurring throughout this build hits INV-02 too: `SuspendLocation` has no reverse command anywhere in the spec's own command list — `SUSPENDED` is a dead end in this v1, stated honestly. `SetQuarantineState`, by contrast, is a real bidirectional toggle in this build — one command that both enters AND releases quarantine (`ACTIVE` ↔ `QUARANTINE`), matching its own "Set...State" naming. `RetireLocation` accepts any of `ACTIVE`/`SUSPENDED`/`QUARANTINE` as its starting status, landing in a real terminal `RETIRED` no command ever leaves.

**Versioned hierarchy:** `inventory_location_hierarchy` is versioned exactly like INV-01's own tracking/valuation policies and AST-02's own `DepreciationSchedule` — a location's parent is never mutated in place, only superseded by a new effective-dated version. `GetLocationAsOf` reads directly from this table, proving the state model's own claim, "hierarchy changes versioned."

**Minimum negative-path acceptance (verbatim, all four):**
- *"Physical location change silently changes legal owner"* — `legal_entity_id` is set once at `CreateInventoryLocation` and is not a field of `AmendLocationMetadataRequest` at all (compile-time). `ReparentLocationControlled` additionally refuses, inside the same transaction as the reparent itself, to attach a location under a parent whose own `legal_entity_id` differs — the same app-level cross-entity check AST-01's own `MergeAssets` uses (a `CHECK` constraint cannot reference another row).
- *"Circular warehouse/bin hierarchy created"* — `ReparentLocationControlled` walks the candidate parent's own current ancestor chain, inside the same transaction, and refuses if the location being reparented appears anywhere in it (including as the candidate parent itself).
- *"Movement enters retired location"* — INV-02 does not itself move stock; its contribution is making `RETIRED` a real terminal status and `ListEligibleLocations` returning `ACTIVE` locations only, for INV-03 (not yet built) to filter against. Stated honestly: the actual movement-time block is deferred to INV-03, the same posture INV-01 already took for its own negative paths #3/#4.
- *"Quarantine released without authority"* — the spec's own SoD, "quarantine release can require independent approval," is enforced the same maker/checker way as every other approval step this session: the principal releasing a quarantine must differ from the principal who set it, refused universally (no finer materiality concept exists in this platform, the same bootstrap-gap posture AST-02's own `ApproveDepreciationRun` took).

**Failure/degradation semantics (verbatim):** "Cross-entity reparenting, circular hierarchy, movement into retired location or unauthorized quarantine release is blocked." All four map 1:1 onto the four negative paths above.

**Dependencies (verbatim):** "ORG; REF; INV-01/03/05; GOV; Security." REF (a canonical location-type/UOM reference) does not exist as a callable service yet — `location_type` is a closed enum today (`WAREHOUSE`/`SITE`/`BIN`/`QUARANTINE_AREA`/`TRANSIT`) hand-derived from the purpose statement itself rather than sourced from REF. INV-03/05 are this same service's own future capabilities.

17 new tests: 11 handler tests (`location_test.go` — invalid location type, duplicate location code, parent-in-different-entity refused at create time, reparent-across-entities refused, circular reparent refused (both a two-hop cycle and a direct self-parent), a valid reparent succeeding, self-quarantine-release refused, release-by-a-different-principal succeeding, missing-reason on quarantine, amend never changes legal entity, retire-from-DRAFT refused, retire-from-ACTIVE succeeds, and eligible-locations-only-returns-ACTIVE) plus 6 real-Postgres tests (`location_store_test.go` — create/get round-trip including confirming a root location has no parent, the real `UNIQUE(tenant_id, legal_entity_id, location_code)` constraint rejecting a duplicate, hierarchy versioning proving a reparent end-dates the old version rather than mutating it (and that an as-of query before the reparent still shows no parent), the real transaction-level cross-entity refusal, the real transaction-level circular-hierarchy refusal, and the full self-release-refused → different-principal-release-succeeds round trip). Verified against a real throwaway Postgres container: both INV-01's and INV-02's migrations apply cleanly in sequence, and the full pre-existing INV-01 suite (14 handler + 5 store) still passes unmodified — 11/11 store tests green. Negative-controlled: temporarily neutered the circular-hierarchy check's own condition (`if false && current == locationID`), confirmed `TestPgStore_ReparentLocation_Circular_Refused` failed exactly as expected, restored, confirmed green again (11/11). No real bugs surfaced this pass — every store method matched its stub's behavior exactly against real Postgres.

**Deliberately out of scope, stated honestly:** `GetLocationInventorySummary` (one of the spec's own five named queries) is not built — it requires INV-03/04's own quantity/valuation data, which does not exist yet; building it now would mean fabricating numbers. `GetLocationHierarchy` is built returning the current ancestor chain only, not descendants/full subtree. `Suspended` has no reverse command anywhere in the spec (see state model above) — a location suspended in this v1 stays suspended. Negative-stock/counting attributes ("counting/negative-stock attributes" in the fuller-ownership field) are not modeled — that authority belongs to INV-05 (Stock Count), not yet built.

Next: INV-03 (Inventory Movement), the third of five Inventory capabilities, in the same service.

---

### §6 INV-03 Inventory Movement — built from scratch (2026-09-09)

Same service, `inventory-management-svc` — INV-03 is the third of five Inventory capabilities co-located here, and the most structurally significant so far: it introduces this service's first cross-service HTTP dependency (financial-close-svc, via a new `internal/clients` package mirroring `asset-management-svc`'s own AST-03 client) and the two negative paths INV-01 and INV-02 each explicitly deferred to it.

**Authority matrix row (verbatim):** "INV-03 | InventoryMovement | Monetary valuation policy or direct GL writes." Purpose (verbatim): "Record immutable quantity movements across external boundaries and locations so on-hand quantity is derived from events rather than editable balance fields." Fuller ownership: "InventoryMovement; movement line; source/destination; quantity/UOM; item/lot/serial; business/effective/posting dates; source reference; movement type; reversal/supersession link."

**No on-hand balance table exists anywhere in this schema.** `GetOnHand`/`GetOnHandAsOf` are always computed live via `SUM` over committed movements — the literal, structural answer to the purpose statement's own final clause. A stored, updatable balance would have been exactly the bug that sentence rules out.

**State model (verbatim):** "Draft→Validated→Committed→Valued→Reconciled; committed movement immutable; reversed/superseded separately." No command reaches `Valued`/`Reconciled` — both depend on INV-04 (Valuation), not yet built — left unreachable, the same posture as AST-02's own gap. "Committed movement immutable" is enforced as a full block (`trg_reject_movement_mutation` refuses ANY update to a `COMMITTED` row, not just an economic-field subset like AST-02/03's own guards) — `ReverseMovement`/`SupersedeMovement` never touch the original row; each creates a brand-new movement with source/destination mechanically swapped (the real inverse of any movement type) and commits it in the same call.

**Minimum negative-path acceptance (verbatim, all four):**
- *"Duplicate receipt creates duplicate quantity"* — per the spec's own failure semantics ("Duplicate idempotency key returns original result"), `CreateInventoryMovement` is a real idempotent create (`UNIQUE(tenant_id, source_idempotency_key)` + `ON CONFLICT DO NOTHING`, resolving to the existing row on conflict) — a retried call is NOT an error, it returns the original movement.
- *"Serial-numbered unit appears in two locations"* — `inventory_serial_residency` tracks the one location a serial currently resides at. `CommitMovement` refuses to receive an already-resident serial, and refuses to ship/transfer a serial that isn't resident at the claimed source — both inside the same transaction as the commit.
- *"Negative stock allowed despite policy prohibition"* — `CommitMovement` computes live on-hand for the source location before allowing any quantity-decreasing movement and refuses if it would go below zero. No per-item/location override policy exists yet — refused universally, the same bootstrap-gap posture this session has hit repeatedly.
- *"Hard-closed movement backdated without correction path"* — `CommitMovement` (called from the handler, before the store is even asked) checks financial-close-svc's real period-status endpoint via the new `internal/clients.Clients.CheckPeriodOpen`, mirroring `asset-management-svc`'s own AST-03 client exactly (unknown period defaults open, unreachable service fails closed).

**Two deferred negative paths land here for real:** INV-01's own "Lot-tracked item moved without lot identity" (migration 000001, negative path #3) and INV-02's own "Movement enters retired location" (migration 000002, negative path #3) are both enforced by `ValidateMovement` — the first place stock actually moves. `ValidateMovement` also refuses an item that isn't `ACTIVE` and a `uom` that doesn't match the item's own `base_uom` ("UOM ambiguity blocks commit").

**SoD (verbatim):** "Manual adjustment/reversal above threshold requires independent approval; source domain cannot create arbitrary inventory adjustment disguised as receipt/issue." No threshold/materiality concept exists platform-wide, so reversal/adjustment authorization uses a distinct action (`INVENTORY_MOVEMENT_ADJUST`/`INVENTORY_MOVEMENT_REVERSE`) separate from plain creation (`INVENTORY_MOVEMENT_CREATE`) — `AdjustInventoryFromApprovedCount` cannot be reached through `ReceiveInventory`/`IssueInventory`'s own code paths, the real structural answer to "disguised as receipt/issue."

**Dependencies (verbatim):** "INV-01/02/04/05; AP receipts; AR fulfillment; PRJ-02; ACC; TAX; GOV; Evidence." INV-01/02 are called in-process (same service, same database). INV-04/05 don't exist yet. AP/AR/PRJ-02 are external source domains with no live movement-triggering hook yet — `source_reference` is caller-declared evidence only, the same bootstrap-gap posture used throughout this session. ACC is the new financial-close-svc dependency described above (a period check only in this v1 — no GL posting; INV-03 explicitly must never own "direct GL writes").

29 new tests: 21 handler tests (`movement_test.go` — both RECEIPT/ISSUE field-shape violations, duplicate-idempotency-key-returns-original, lot-required-but-missing, movement-into-a-retired-location, duplicate-serial-receipt, issue-more-than-on-hand, period-locked, a full receive→validate→commit lifecycle confirming on-hand, a reversal reverting on-hand back to zero, missing-reason on reverse, and adjustment using a distinct authz action) plus 8 real-Postgres tests (`movement_store_test.go` — idempotent-create-returns-original, a full lifecycle confirming live on-hand, the real reject-mutation trigger refusing a raw `UPDATE` against a `COMMITTED` row, the real negative-stock refusal, the real serial-residency refusal, and a full reversal round trip proving the mechanical source/destination swap against real Postgres). Verified against a real throwaway Postgres container: all three migrations apply cleanly in sequence, and the full pre-existing INV-01/INV-02 suite (25 handler + 11 store) still passes unmodified — 17/17 store tests green on the first run, no bugs found this pass. Negative-controlled: temporarily neutered the negative-stock check's own condition (`if false && onHand-m.Quantity < 0`), confirmed `TestPgStore_CommitMovement_NegativeStockRefused` failed exactly as expected, restored, confirmed green again (17/17).

**Deliberately out of scope, stated honestly:** `TransferInventory`'s own on-hand math and `AdjustInventoryFromApprovedCount`'s own direction convention (destination = increase, source = decrease, reusing RECEIPT/ISSUE's own field shape rather than a dedicated signed-delta field) are real but not spec-literal — the spec names the commands without specifying their exact field shape. `InventoryMovementExceptionRaised` (one of the spec's own five named events) is not wired in — this v1 has no exception-detection logic. `GetMovementLineage` is served today by `ListMovements`, real and queryable, but not framed as a dedicated lineage-narrative endpoint. Concurrent commits against the same (item, location) are not explicitly locked beyond each commit's own transaction boundary — a real race window exists under high concurrency, not exercised by these tests (single-writer assumption, consistent with this session's other balance-guard implementations).

Next: INV-04 (Inventory Valuation), the fourth of five Inventory capabilities, in the same service.

---

### §7 INV-04 Inventory Valuation — built from scratch (2026-09-09)

Same service, `inventory-management-svc` — INV-04 is the fourth of five Inventory capabilities, and this service's first capability to post to general-ledger-svc (a new `internal/clients/ledger.go`, mirroring `asset-management-svc`'s own AST-02/03 clients).

**Authority matrix row (verbatim):** "INV-04 | InventoryCostLayer/Pool | Physical quantity truth or commercial pricing." Purpose (verbatim): "Assign deterministic monetary cost to inventory movements and on-hand stock using effective-dated accounting-policy methods and source-linked cost evidence." Fuller ownership: "InventoryCostLayer/Pool; ValuationEntry; unit cost; inventory value; consumed cost; landed/conversion cost allocation; write-down/reversal; valuation run/version."

**State model (verbatim):** "Movement valuation Draft→Calculated→Validated→Final; run PopulationFrozen→Calculated→Approved→AccountingEventEmitted→Reconciled; adjustments supersede prior state." The same doc-vs-command mismatch pattern recurring throughout this build hits INV-04 hardest: `ValueMovement` is the ONLY command touching a movement's own valuation and lands directly in FINAL (the same collapse `BuildDepreciationSchedule` already used). On the run side, `CreateValuationRun` folds "PopulationFrozen" into creation itself (claiming every unclaimed FINAL entry for that period in the same transaction), and `EmitInventoryAccountingEvent` collapses Calculated/Approved into the one command that actually posts — no separate approve command is named anywhere in the spec's own command list, so the spec's own SoD requirement is satisfied by requiring the senior `inventory.valuation.approve` action on the one command that exists, rather than inventing an unnamed intermediate step.

**Scope narrowing, stated honestly up front:** of the eight explicit commands, this v1 builds `ValueMovement`, `CreateValuationRun`, `EmitInventoryAccountingEvent`, `RecordInventoryWriteDown` and `ReverseWriteDown` as real, working commands. `RecalculateValuation`, `AllocateLandedCost` and `RebuildCostLayersControlled` are deliberately deferred — each is real, non-trivial follow-on work that would roughly double this migration's own scope.

**"Method not permitted by active accounting policy blocks valuation" is satisfied for free:** LIFO was never added to INV-01's own `ValuationMethod` enum (FIFO/WEIGHTED_AVERAGE/STANDARD_COST only) — there is no code path anywhere in this platform that could value a movement using a prohibited method, because that method does not exist. This directly satisfies negative path #1, "IFRS book uses prohibited LIFO profile."

**Minimum negative-path certification (verbatim, all four):**
- *"IFRS book uses prohibited LIFO profile"* — see above; structurally impossible, not merely refused.
- *"Late landed cost silently rewrites closed-period COGS"* — `ValueMovement`/`EmitInventoryAccountingEvent`/`RecordInventoryWriteDown` all call the same financial-close-svc period check INV-03 already built, and `inventory_valuation_entries` has its own `UNIQUE(tenant_id, movement_id)` making any entry a one-time, append-only fact — there is no code path that could "rewrite" one.
- *"NRV write-down lacks evidence"* — `valuation_evidence_ref` is a real `NOT NULL` column and a real application-level 422, not a documented convention.
- *"Same movement consumes two cost layers twice"* — `UNIQUE(tenant_id, source_movement_id)` on `inventory_cost_layers` and `UNIQUE(tenant_id, movement_id)` on `inventory_valuation_entries` together make it structurally impossible for one movement to ever create or consume layers twice — a second `ValueMovement` call against the same movement is refused outright.

**FIFO/weighted-average consumption, real and tested:** an OUTBOUND movement (ISSUE, TRANSFER's source side, or a decreasing ADJUSTMENT) consumes `inventory_cost_layers` in `created_at ASC` (FIFO) order, `SELECT ... FOR UPDATE` inside the same transaction as the commit. FIFO values each consumed slice at that layer's own cost; WEIGHTED_AVERAGE consumes the same layers/quantities for bookkeeping but values the whole consumption at a single blended rate computed across every remaining layer just before that consumption — a real, defensible simplification of perpetual weighted-average (documented as a scope choice, not a shortcut). STANDARD_COST requires a caller-declared unit cost on the request itself, since no standard-cost master exists yet — the same bootstrap-gap posture used throughout.

**GL posting is deliberately asymmetric, stated honestly:** `EmitInventoryAccountingEvent` posts only the OUTBOUND (COGS) side of a run's frozen population — debit COGS, credit Inventory, the one real, balanced entry this capability can make on its own. INBOUND entries (inventory value added by RECEIPT/ADJUSTMENT-increase movements) are NOT posted here: a balanced entry for them needs an offsetting AP/GRNI accrual account this platform does not model yet, and posting an unbalanced entry would be worse than not posting at all.

**A real, deliberate gap between INV-03 and INV-04:** INV-03's own negative-stock check only knows about PHYSICAL movements. A RECEIPT that is never valued (no cost layer created) still lets a same-sized ISSUE commit at the physical layer — but valuing that ISSUE afterward has nothing to draw from. `ErrInsufficientCostLayers` is the real, distinct guard for this — tested explicitly, both at the handler and store layers.

21 new tests: 11 handler tests (`valuation_test.go` — inbound missing unit cost, inbound creates a real cost layer and updates `GetInventoryValue`, valuing the same movement twice refused, a full FIFO consumption across two differently-priced layers, insufficient-cost-layers refused, run-emit self-approval refused, a different approver's emit posting exactly one journal, write-down missing evidence refused, write-down with evidence posting exactly one journal, write-down self-reversal refused, and reversal-by-a-different-principal posting exactly one reversal) plus 10 real-Postgres tests (`valuation_store_test.go` — inbound cost-layer creation, the real `UNIQUE(tenant_id, source_movement_id)`/`UNIQUE(tenant_id, movement_id)` constraints refusing a second valuation, real FIFO consumption order and value math against actual Postgres including the `ORDER BY created_at ASC ... FOR UPDATE` query, the real insufficient-layers refusal, a full valuation-run lifecycle (freeze → emit → verify 2 claimed entries), and write-down create/reverse/re-reverse-refused). Verified against a real throwaway Postgres container: all four migrations apply cleanly in sequence, and the full pre-existing INV-01/02/03 suite (40 handler + 17 store) still passes unmodified — 23/23 store tests green after fixes (below). Negative-controlled: temporarily neutered the insufficient-cost-layers check's own condition (`if false && totalRemaining < quantity`), confirmed `TestPgStore_ValueMovement_InsufficientLayers_Refused` failed exactly as expected, restored, confirmed green again (23/23).

Three real bugs caught by the real-Postgres pass, not by inspection: (1) valuing an already-valued INBOUND movement a second time hit the `inventory_cost_layers` table's own `UNIQUE(tenant_id, source_movement_id)` constraint with a raw, unmapped Postgres error instead of the intended `ErrMovementAlreadyValued` — fixed by catching that specific unique-violation and mapping it, same as the entries-table half of the same guard. (2) `CreateValuationRun`'s own population-freeze query returned 0 frozen entries in a real-DB test because a test helper never set `legal_entity_id` on its constructed movements (defaulting to empty string, invisible to the in-memory stub's own looser matching but a real mismatch against Postgres) — fixed the test helper, not production code, but caught only because the real-DB pass checks a real filter the stub was too permissive to catch. (3) `inventory_write_downs.status` was declared `VARCHAR(20)`, one character too narrow for `ACCOUNTING_EVENT_EMITTED` (24 characters) — the exact same class of bug AST-02's own `depreciation_runs.status` hit in this session; widened to `VARCHAR(30)`.

**Deliberately out of scope, stated honestly:** `RecalculateValuation`, `AllocateLandedCost` and `RebuildCostLayersControlled` (three of the spec's own eight explicit commands) are not built. `Reconciled` (the run state model's own terminal state) has no command anywhere and is not built. `GetCOGSAssignment`, `GetInventoryValueAsOf` and `GetValuationEvidence` (three of the spec's own six named queries) are not yet built as dedicated endpoints — `GetValuationEntry`/`GetCostLayers` serve the underlying data today, real and queryable, just not framed as those exact named queries. INV-04's own five named events (`InventoryValued`, `CostLayerCreated`, `InventoryWriteDownRecorded`, `InventoryWriteDownReversed`, `InventoryAccountingEventEmitted`) are wired into the `Publisher` interface EXCEPT `CostLayerCreated` — a real, stated gap. REF FX (currency conversion) is not modeled — every value in this v1 is treated as a single, unconverted currency. `RecordInventoryWriteDown`'s own "approval" is satisfied by requiring a senior action to create it at all rather than a genuine two-step create-then-approve flow, since no command for the latter exists anywhere in the spec.

Next: INV-05 (Stock Count), the fifth and final Inventory capability, in the same service.

---

### §8 INV-05 Stock Count — built from scratch (2026-09-09)

Same service, `inventory-management-svc` — INV-05 is the fifth and final Inventory capability, closing out the Inventory sub-domain (INV-01 through INV-05 all complete). It integrates in-process with all four prior capabilities: INV-01 (item eligibility), INV-02 (location scope), INV-03 (the ONLY path to an on-hand change), and INV-04 (an approved variance is eligible for INV-04 valuation once generated, though this v1 doesn't auto-trigger it — stated below).

**Authority matrix row (verbatim):** "INV-05 | StockCountSession | Direct on-hand edits or valuation/accounting policy." Purpose (verbatim): "Govern physical/cycle count populations, observations, recounts and variance approvals while requiring all accepted quantity corrections to flow through INV-03 movements." A cross-reference row elsewhere in the spec states the whole architecture in one line: "Count variance is not a direct balance edit — INV-05 approves variance; INV-03 creates the adjustment movement; INV-04 values it; ACC-04 posts any accounting consequence."

**State model (verbatim):** "Planned→PopulationFrozen→Counting→VarianceReview→Recount/Approved→AdjustmentsGenerated→Reconciled→Certified; Cancelled before certification." The same doc-vs-command mismatch pattern recurring throughout this build hits INV-05 too: there is no command that reaches "Counting" independently of `RecordBlindCount` itself — recording the first observation IS what moves a line into Counting, no separate "start counting" command exists. "Reconciled" has no command anywhere and is left unreachable, the same posture as AST-02's own Reconciled/Certified gap — `CertifyStockCount` reaches `CERTIFIED` directly from `ADJUSTMENTS_GENERATED`.

**Minimum negative-path certification (verbatim, all four):**
- *"Counter sees system quantity in blind count"* — `RecordBlindCount`'s own response type, `domain.RecordedBlindCount`, has no field for `system_quantity` at all — a compile-time absence, not a runtime redaction. Verified directly in a handler test that inspects the raw response body for the string `system_quantity`.
- *"Population changes after freeze without invalidation"* — `inventory_stock_count_lines`' own economic fields (`item_id`, `location_id`, `system_quantity`) are written exactly once, at freeze time, and `trg_reject_count_line_population_mutation` blocks any later `UPDATE` to them — verified with a real negative control and a real raw-`UPDATE` rejection test against Postgres.
- *"Variance approved by same counter"* — `ApproveCountVariance` refuses if the approving principal equals that SPECIFIC line's own `observed_by_principal_id` — maker/checker at the line level, not the whole count session, enforced inside the same guarded-transition transaction (the same pattern INV-02's own `SetQuarantine` release check uses).
- *"Count directly overwrites on-hand quantity"* — `GenerateAdjustmentMovements` is the ONLY path from an approved variance to an on-hand change, and it works by calling INV-03's own real `CreateMovement`/`ValidateMovement`/`CommitMovement` in-process — none of this capability's own tables has a column that could hold an on-hand quantity at all; `adjustment_movement_id` only ever links to a real, committed row in `inventory_movements`.

**The frozen population is real, not declared:** `FreezeCountPopulation` doesn't take an item list from the caller — it derives one, querying every item that has EVER moved through each of the count's own scoped locations (a real `SELECT DISTINCT` over `inventory_movements`), then computes each (item, location) pair's live on-hand via the same `SUM`-over-committed-movements formula INV-03's own `GetOnHand` uses — inlined here for transactional atomicity with the freeze itself, so the snapshot and the freeze can never disagree.

**SoD (verbatim):** "Counter cannot approve own material variance; system quantity hidden during blind count; adjustment generator cannot bypass INV-03 approval policy." All three map directly onto negative paths #3, #1, and #4 above — `GenerateAdjustmentMovements` cannot "bypass INV-03 approval policy" because it IS INV-03's own real commands, not a shortcut around them; a hard-closed-period or negative-stock adjustment is refused by INV-03's own guards exactly as if a human had called `IssueInventory`/`ReceiveInventory` directly.

22 new tests: 11 handler tests (`stock_count_test.go` — missing-locations create refused, freeze capturing the real system quantity, the blind-count response body inspected for the absence of `system_quantity`, self-approval refused, approval by a different principal succeeding, re-freezing an already-frozen count refused, a full generate-adjustments call producing a real COMMITTED ADJUSTMENT movement with the correct direction/quantity/item, no-variance-approved-lines refused, certify-before-adjustments-generated refused, missing-reason cancel refused, and cancel-before-certification succeeding) plus 4 real-Postgres tests (`stock_count_store_test.go` — the real system-quantity snapshot against actual committed movements, the real reject-mutation trigger refusing a raw `UPDATE` to `system_quantity`, the real self-approval refusal at the line level, and a full count→freeze→count→approve→generate-a-real-movement→certify lifecycle proving the on-hand value ends at exactly 25 (20 + a 5-unit real INV-03 adjustment) via `GetOnHand`, never a value this capability wrote itself). Verified against a real throwaway Postgres container: all five migrations apply cleanly in sequence, and the full pre-existing INV-01/02/03/04 suite (51 handler + 23 store) still passes unmodified — 27/27 store tests green on the first run, no new bugs found this pass. Negative-controlled: temporarily neutered the population-immutability trigger's own condition (wrapped in `IF FALSE AND (...)`), confirmed `TestPgStore_FreezeCountPopulation_LinesImmutable` failed exactly as expected, restored, confirmed green again (27/27).

**Deliberately out of scope, stated honestly:** `GetVarianceReport`, `GetCountSnapshot`, `GetAdjustmentStatus` and `GetCountEvidence` (four of the spec's own six named queries) are not built as dedicated endpoints — `GetStockCount` serves the same underlying data today, real and queryable, just not framed as those exact named reports. `StockCountVarianceDetected` (one of the spec's own six named events) is not wired in. `GenerateAdjustmentMovements` does NOT automatically call INV-04's own `ValueMovement` on the adjustment it creates — the generated movement sits COMMITTED-but-unvalued until a caller separately values it through INV-04's existing endpoints, the same "each capability's own explicit command, never an implicit side effect of another" posture this session has held throughout. Tolerance/materiality thresholds ("Segregation of duties... Counter cannot approve own MATERIAL variance") are not modeled — self-approval is refused universally regardless of variance size, the same "no finer materiality concept exists yet" bootstrap gap this session has hit repeatedly. `RequestRecount` is built as a real status transition but nothing currently distinguishes a recount-triggering variance from any other — any `COUNTED` line can be sent back to `NEEDS_RECOUNT`.

This closes the entire Inventory sub-domain (INV-01 through INV-05, all five capabilities complete in `inventory-management-svc`). Next: PRJ-01 (Project/Job Master), the first of four Project Accounting capabilities, in a new `project-accounting-svc`.

---

### §9 PRJ-01 Project / Job Master — built from scratch (2026-09-09)

New service, `project-accounting-svc` (port 8169, DB `project_accounting`) — the first of four Project Accounting capabilities, scaffolded from `inventory-management-svc`'s own layout the same way that service was scaffolded from `consolidation-svc`.

**Authority matrix row (verbatim):** "PRJ-01 | FinancialProject | General task/project-management execution or cross-entity accounting aggregate." Purpose (verbatim): "Maintain the financial project/job identity, WBS/work-package structure and accounting-policy bindings required for project costing and revenue/WIP without becoming a general project-management task system." Fuller ownership: "FinancialProject; ProjectWorkPackage/WBS; legal entity; customer/contract references; project type; manager/cost center; accounting/billing policy refs; dimensions; lifecycle."

**State model (verbatim):** "Draft→Approved→Active→Suspended→Closing→Closed; Reopened controlled. WBS versions effective-dated." The same doc-vs-command mismatch pattern recurring throughout this build hits PRJ-01 too: no command reaches "Closing" independently of `CloseProject` itself (the same collapse AST-01's own `ApproveAssetRegistration` already used). `SuspendProject` has no reverse command anywhere in the spec's own command list — `SUSPENDED` is a dead end in this v1, the same posture INV-02's own `SuspendLocation` already took. `ReopenProjectControlled` is real and distinct — it is the `CLOSED` → `ACTIVE` bridge, never a bridge out of `SUSPENDED`.

**Minimum negative-path certification (verbatim, all four):**
- *"Single project posts costs across two legal entities"* — a project has exactly one `legal_entity_id` column, set once at `CreateProject` and never editable by any later command, not even `AmendFinancialProfile` — there is no schema path for a second entity to ever attach to one project. "Group programs use separate entity projects with group reference" is satisfied by `group_reference`: a caller-declared, evidence-only correlation key across separately-owned, single-entity projects, never a shared accounting aggregate. The real cost-posting half of this negative path belongs to PRJ-02 (not yet built), stated honestly as deferred — the same posture INV-01/02 took for negative paths their own later capabilities finish.
- *"Recognition policy changed after run without invalidation"* — `project_financial_profiles` is versioned exactly like INV-01's own `ValuationPolicy`: `AmendFinancialProfile` only ever accepts an `effective_from` strictly later than the moment it runs — there is no in-place edit path to a profile's own `recognition_method`.
- *"Closed project accepts new cost without controlled reopen"* — `CLOSED` is only ever reached via `CloseProject` and only ever left via `ReopenProjectControlled`, which refuses self-reopen (the spec's own SoD: "closed project reopen requires independent approval") — the same maker/checker posture used throughout this session.
- *"WBS deletion orphans historical cost"* — there is no delete (or even amend) command for a work package anywhere in the spec's own command list; `AddWorkPackage` is the only write path, and `trg_reject_wbs_mutation` makes every work package row permanently immutable once created — verified against real Postgres with both a raw `UPDATE` and a raw `DELETE`, both refused.

**SoD (verbatim):** "Project creator cannot self-approve high-value/regulated project profile; closed project reopen requires independent approval; project manager cannot alter recognition policy after approved revenue runs without invalidation." The first two map directly onto real maker/checker guards (`ApproveProject`, `ReopenProjectControlled`); the third is satisfied by the same future-effective-only enforcement that answers negative path #2 — there is no "after approved revenue runs" distinction to make yet since PRJ-03 (the thing that would run revenue recognition) doesn't exist, so the guard is applied universally rather than conditionally, the same bootstrap-gap posture used throughout this session.

**Failure/degradation semantics (verbatim):** "Project cannot span multiple legal entities as one accounting aggregate; group programs use separate entity projects with group reference. Missing policy/currency/book blocks activation." The second half is `ActivateProject`'s own real gate: refuses (422, `ErrFinancialProfileRequiredForActivation`) without a current financial profile — the same pattern INV-01's own `ActivateInventoryItem` uses for its own valuation policy.

19 new tests: 14 handler tests (`handler_test.go` — missing-fields create refused, duplicate-project-code refused, self-approval refused, approval by a different principal succeeding, activation-without-financial-profile refused, activation-with-profile succeeding, retroactive profile change refused, invalid recognition method refused, missing-reason close refused, self-reopen refused, reopen-by-a-different-principal succeeding, duplicate WBS code refused, missing-contract-ref link refused, and an authorization-denied path) plus 5 real-Postgres tests (`pg_store_test.go` — create/get round-trip, the real `UNIQUE(tenant_id, legal_entity_id, project_code)` constraint rejecting a duplicate, the real self-reopen refusal followed by a different-principal reopen succeeding, the real reject-mutation trigger refusing both a raw `UPDATE` and a raw `DELETE` against a work package, and financial-profile versioning proving a retroactive `GetFinancialProfileAsOf` query still returns the historic version). Verified against a real throwaway Postgres container (`postgres:16-alpine`, torn down after use): all 5 store tests pass on the first run, no bugs found this pass. Negative-controlled: temporarily neutered the self-reopen check's own condition (`if false && closedBy != nil && ...`), confirmed `TestPgStore_ReopenProjectControlled_SelfReopenRefused` failed exactly as expected, restored, confirmed green again (5/5).

**Deliberately out of scope, stated honestly:** `GetProjectAsOf` is built as a query parameter (`?at=`) on `GET /v1/projects/{id}/as-of` rather than a dedicated named endpoint per the spec's query list, but is otherwise fully real. `LinkContract` records a caller-declared `contract_ref` only — no live BIZ contract service exists yet to validate against, the same bootstrap-gap posture used throughout this session. Negative path #1's own cost-posting enforcement (validating a captured cost's source entity against the project's own `legal_entity_id`) is real only once PRJ-02 exists to capture costs at all — PRJ-01 itself has no cost-capture surface, so there is nothing yet that COULD violate it; the schema-level single-entity guarantee is what's real today. `dimensions` (named in the fuller-ownership field) is not modeled — no REF dimension service exists platform-wide.

Next: PRJ-02 (Project Cost Capture), the second of four Project Accounting capabilities, in the same service.

---

### §10 PRJ-02 Project Cost Capture — built from scratch (2026-09-09)

Same service, `project-accounting-svc` — PRJ-02 is the second of four Project Accounting capabilities, and the most cross-cutting one in the sub-domain: it attributes costs originating in AP, payroll, inventory (INV-03/04), assets (AST-02/03) and allocations onto projects.

**Authority matrix row (verbatim):** "PRJ-02 | ProjectCostEntry | Source AP/payroll/inventory facts or duplicate source GL posting." Purpose (verbatim): "Capture source-linked project/job cost attribution from AP, expenses, payroll, inventory, assets and allocations without double-posting costs already recognized by source domains." The doc's own Architecture Decisions section states the design intent in one line: "Project cost capture does not double-post source costs — PRJ-02 attributes authoritative AP/payroll/inventory/asset costs to projects. Only incremental reclassification/capitalization effects produce new accounting events."

**State model (verbatim):** "Captured→Validated→Accepted→Reconciled; Reclassified/Reversed via linked entries; source accounting state remains separate." The same doc-vs-command mismatch pattern recurring throughout this build hits PRJ-02 twice: no command reaches "Accepted" independently of `ValidateProjectCost` (the same collapse `ValidateDepreciationRun` already applied to Run's own Calculated state), and "Reconciled" has no command anywhere and is left unreachable, the same posture as AST-02's own gap. "Reclassified/Reversed via linked entries" is the spec's own words for exactly the append-only correction-chain pattern used repeatedly this session.

**Minimum negative-path certification (verbatim, all four):**
- *"Same AP line captured twice to project"* — per the spec's own failure semantics ("Duplicate source line is idempotently rejected/returned"), `CaptureProjectCost` is a real idempotent create backed by `UNIQUE(tenant_id, source_type, source_reference)` — a retried capture returns the ALREADY-CAPTURED entry rather than erroring or creating a second one, the same pattern INV-03's own `CreateMovement` uses.
- *"Source payroll reversal not reflected"* — `ReverseProjectCost` is the real, working command a source-domain reversal triggers, creating a linked negative-amount entry that nets the original out of every aggregate query (verified end-to-end against real Postgres: net total is exactly 0 after reversal). No live payroll/AP/INV/AST integration exists yet to call it AUTOMATICALLY on a source reversal — stated honestly as a deferred bootstrap gap, which PRJ-02's own Dependencies field ("WFP/Payroll") anticipates.
- *"Project cost capture creates duplicate GL expense"* — this service has **no general-ledger-svc client anywhere**; `CaptureProjectCost`/`ValidateProjectCost`/`CertifyCostPopulation` never call any posting endpoint. Structurally impossible to double-post because no posting code path exists at all — matching the authority matrix's own words, "must never own ... duplicate source GL posting."
- *"Manual cost amount differs from authoritative source without approved adjustment"* — `ReclassifyProjectCost` is the ONLY manual-adjustment path (capture is always source-driven), uses its own distinct authz action (`PROJECT_COST_ADJUST`, not `PROJECT_COST_CAPTURE`) and refuses self-approval universally — an unapproved amount difference is structurally impossible to write.

**Append-only with a correctly-scoped trigger:** `trg_reject_cost_entry_economic_mutation` blocks changes to the seven economic fields (`source_type`, `source_reference`, `project_id`, `wbs_id`, `amount`, `currency`, `transaction_date`) while deliberately still allowing `status`/`billable`/`capitalizable`/approval-metadata updates — verified both ways in one real-Postgres test: a raw `UPDATE` to `amount` is refused, and `ValidateProjectCost` (a status-only update) succeeds in the same test, proving the trigger is scoped, not a blanket block.

**SoD (verbatim):** "Manual project-cost adjustment above threshold requires independent approval; project cost user cannot modify source AP/payroll/inventory facts." The first is the maker/checker guard above (no materiality tiering exists platform-wide, so applied universally). The second is satisfied trivially and completely: this service's schema has no table, column, or code path that could write to another service's database — cross-service writes on this platform only ever happen through that service's own HTTP API, and PRJ-02 calls none.

24 new tests: 12 handler tests (`cost_entry_test.go` — missing source_type, duplicate-source-reference-returns-original, validate succeeding, mark-billable succeeding, self-approval refused on both reclassify and reverse, reclassify by a different principal creating a properly-linked entry while leaving the original untouched, missing-reason reclassify refused, reversal netting to zero across a real list query, double-reversal refused, a structural assertion that capture needs no ledger dependency at all, and certification excluding a reversed original) plus 10 real-Postgres tests total in the service (5 new in `cost_entry_store_test.go` — idempotent capture, the correctly-scoped immutability trigger proven both ways, the real self-approval refusal, a full reversal netting to zero plus double-reversal refused, and the real certification query). Verified against a real throwaway Postgres container: both migrations apply cleanly in sequence, and the full pre-existing PRJ-01 suite (14 handler + 5 store) still passes unmodified — 10/10 store tests green after fixes. Negative-controlled: temporarily neutered the economic-mutation trigger's own condition (wrapped in `IF FALSE AND (...)`), confirmed `TestPgStore_CostEntry_EconomicFieldsImmutable` failed exactly as expected, restored, confirmed green again (10/10).

Two real bugs caught by the real-Postgres pass, not by inspection: (1) `costEntryColumns` selected 23 columns while `scanCostEntry` supplied only 22 destinations — `tenant_id` was in the SELECT list but `CostEntry` has no field for it (the in-memory stub never exercises column/destination arity at all); fixed by dropping the redundant column from the list, matching how INV-04's own valuation-entry columns are shaped. (2) `CostCategory` is a plain `string` field against a nullable column, so a NULL `cost_category` failed with "cannot scan NULL into *string"; fixed with a nullable scratch pointer, the same pattern INV-01's own `scanItem` already uses for its own optional fields.

**Deliberately out of scope, stated honestly:** no live source-domain integrations exist — every cost is caller-declared against a `source_reference`, with no automatic ingestion from AP/payroll/INV-03/AST-02/03 (the spec's own Dependencies field names services that mostly don't exist yet, and the two that do — inventory-management-svc and asset-management-svc — have no outbound cost-notification hook). `GetCostSourceLineage`, `GetUnallocatedCostExceptions` and `GetProjectCostAsOf` (three of the spec's own five named queries) are not built as dedicated endpoints; `GetProjectCosts`/`GetCostByWBS` are served by one `ListCostEntries` endpoint with an optional `?wbs_id=` filter. FX conversion is not modeled — each entry keeps its own declared currency with no cross-currency aggregation. `AllocateSharedCostToProject` is a thin wrapper that pre-sets `source_type=ALLOCATION` rather than a real allocation-basis engine — "allocation basis" is captured as evidence only, not computed.

Next: PRJ-03 (Project Revenue & WIP), the third of four Project Accounting capabilities, in the same service.

---
