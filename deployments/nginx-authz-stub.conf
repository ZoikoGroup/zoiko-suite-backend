server {
    listen 80;

    # Stub authorization service — returns {"allowed":true} for every request.
    # Used in Phase 5 local dev stack only. NOT for production.
    location / {
        add_header Content-Type application/json always;
        return 200 '{"allowed":true,"reason":"stub"}';
    }
}
