apiVersion: v1
kind: Service
metadata:
  name: workflow-history-svc
  namespace: zoiko-evidence
  labels:
    app: workflow-history-svc
spec:
  ports:
  - port: 8097
    targetPort: 8097
    name: http
  selector:
    app: workflow-history-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-history-svc
  namespace: zoiko-evidence
  labels:
    app: workflow-history-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workflow-history-svc
  template:
    metadata:
      labels:
        app: workflow-history-svc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
      - name: workflow-history-svc
        image: workflow-history-svc:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8097
          name: http
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        env:
        - name: PORT
          value: "8097"
        - name: DB_HOST
          value: "postgres.zoiko-infra.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "workflow_history"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "postgres"
        - name: DB_SSLMODE
          value: "disable"
        - name: KAFKA_BROKERS
          value: "kafka.zoiko-infra.svc.cluster.local:9092"
        - name: KAFKA_GROUP_ID
          value: "workflow-history-svc"
        - name: KAFKA_TOPIC
          value: "zoiko.workflow.events"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8097
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8097
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
