apiVersion: v1
kind: Service
metadata:
  name: authorization-svc
  namespace: zoiko-governance
  labels:
    app: authorization-svc
spec:
  ports:
  - port: 8089
    targetPort: 8089
    name: http
  selector:
    app: authorization-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: authorization-svc
  namespace: zoiko-governance
  labels:
    app: authorization-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authorization-svc
  template:
    metadata:
      labels:
        app: authorization-svc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
      - name: authorization-svc
        image: authorization-svc:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8089
          name: http
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        env:
        - name: PORT
          value: "8089"
        - name: DB_HOST
          value: "postgres.zoiko-infra.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "authorization_svc"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "postgres"
        - name: DB_SSLMODE
          value: "disable"
        - name: KAFKA_BROKERS
          value: "kafka.zoiko-infra.svc.cluster.local:9092"
        - name: KAFKA_GROUP_ID
          value: "authorization-svc"
        - name: KAFKA_EVENTS_TOPIC
          value: "zoiko.authorization.events"
        - name: JURISDICTION_RULES_URL
          value: "http://jurisdiction-svc.zoiko-governance.svc.cluster.local:8082"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8089
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8089
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
