###############################################################################
# 37-app-esignature-integration.yaml
# Phase 7 — eSignature Integration Service  (port 8148)
###############################################################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: esignature-integration-svc
  namespace: zoiko-extensions
  labels:
    app: esignature-integration-svc
    phase: "7"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: esignature-integration-svc
  template:
    metadata:
      labels:
        app: esignature-integration-svc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
      containers:
        - name: esignature-integration-svc
          image: zoiko/esignature-integration-svc:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8148
          env:
            - name: PORT
              value: "8148"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: esignature-integration-db-secret
                  key: url
            - name: KAFKA_BROKERS
              value: "kafka:9092"
            - name: KAFKA_EVENTS_TOPIC
              value: "zoiko.esignature.integration.events"
            - name: AUTHZ_SERVICE_URL
              value: "http://authorization-svc.zoiko-governance:8081"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8148
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8148
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: esignature-integration-svc
  namespace: zoiko-extensions
spec:
  selector:
    app: esignature-integration-svc
  ports:
    - protocol: TCP
      port: 8148
      targetPort: 8148
