apiVersion: v1
kind: Service
metadata:
  name: purchase-order-svc
  namespace: zoiko-finance
  labels:
    app: purchase-order-svc
spec:
  ports:
  - port: 8129
    targetPort: 8129
    name: http
  selector:
    app: purchase-order-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: purchase-order-svc
  namespace: zoiko-finance
  labels:
    app: purchase-order-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: purchase-order-svc
  template:
    metadata:
      labels:
        app: purchase-order-svc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
      - name: purchase-order-svc
        image: purchase-order-svc:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8129
          name: http
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        env:
        - name: PORT
          value: "8129"
        - name: DB_HOST
          value: "postgres.zoiko-infra.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "purchase_order"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "postgres"
        - name: DB_SSLMODE
          value: "disable"
        - name: KAFKA_BROKERS
          value: "kafka.zoiko-infra.svc.cluster.local:9092"
        - name: KAFKA_GROUP_ID
          value: "purchase-order-svc"
        - name: KAFKA_EVENTS_TOPIC
          value: "zoiko.purchase-order.events"
        # In-cluster DNS names for authorization-svc and purchase-request-svc.
        # Config defaults are the compose short names, which would NXDOMAIN
        # inside K8s — override explicitly here.
        - name: AUTHZ_SERVICE_URL
          value: "http://authorization-svc.zoiko-governance.svc.cluster.local:8089"
        - name: PURCHASE_REQUEST_SERVICE_URL
          value: "http://purchase-request-svc.zoiko-finance.svc.cluster.local:8100"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.zoiko-infra.svc.cluster.local:4318"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8129
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8129
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
