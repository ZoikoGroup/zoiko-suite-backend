apiVersion: v1
kind: Service
metadata:
  name: policy-svc
  namespace: zoiko-governance
  labels:
    app: policy-svc
spec:
  ports:
  - port: 8085
    targetPort: 8085
    name: http
  selector:
    app: policy-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-svc
  namespace: zoiko-governance
  labels:
    app: policy-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: policy-svc
  template:
    metadata:
      labels:
        app: policy-svc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
      - name: policy-svc
        image: policy-svc:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8085
          name: http
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        env:
        - name: PORT
          value: "8085"
        - name: DB_HOST
          value: "postgres.zoiko-infra.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "policy"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "postgres"
        - name: DB_SSLMODE
          value: "disable"
        - name: KAFKA_BROKERS
          value: "kafka.zoiko-infra.svc.cluster.local:9092"
        - name: KAFKA_GROUP_ID
          value: "policy-svc"
        - name: KAFKA_EVENTS_TOPIC
          value: "zoiko.policy.events"
        - name: GOVERNANCE_DECISION_LOG_SERVICE_URL
          value: "http://governance-svc.zoiko-governance.svc.cluster.local:8083"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8085
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8085
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
