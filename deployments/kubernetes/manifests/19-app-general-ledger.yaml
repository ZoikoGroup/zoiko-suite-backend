apiVersion: v1
kind: Service
metadata:
  name: general-ledger-svc
  namespace: zoiko-finance
  labels:
    app: general-ledger-svc
spec:
  ports:
  - port: 8098
    targetPort: 8098
    name: http
  selector:
    app: general-ledger-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: general-ledger-svc
  namespace: zoiko-finance
  labels:
    app: general-ledger-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: general-ledger-svc
  template:
    metadata:
      labels:
        app: general-ledger-svc
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
      - name: general-ledger-svc
        image: general-ledger-svc:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8098
          name: http
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        env:
        - name: PORT
          value: "8098"
        - name: DB_HOST
          value: "postgres.zoiko-infra.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "general_ledger"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "postgres"
        - name: DB_SSLMODE
          value: "disable"
        - name: KAFKA_BROKERS
          value: "kafka.zoiko-infra.svc.cluster.local:9092"
        - name: KAFKA_GROUP_ID
          value: "general-ledger-svc"
        - name: KAFKA_EVENTS_TOPIC
          value: "zoiko.general-ledger.events"
        # In-cluster DNS name for authorization-svc.
        # Config default is "http://authorization-svc:8089" which is the compose
        # short name and would NXDOMAIN inside K8s — override explicitly here.
        - name: AUTHZ_SERVICE_URL
          value: "http://authorization-svc.zoiko-governance.svc.cluster.local:8089"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.zoiko-infra.svc.cluster.local:4318"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8098
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8098
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
